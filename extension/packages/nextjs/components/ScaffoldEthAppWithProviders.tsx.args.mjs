export const preContent = `import { SessionProvider } from "next-auth/react";`;

export const extraProviders = {
  "SessionProvider": {}
};

export const globalClassNames = "";

export const overrideProviders = {};
